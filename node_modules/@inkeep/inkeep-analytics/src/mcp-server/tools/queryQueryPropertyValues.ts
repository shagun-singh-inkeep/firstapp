/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { queryQueryPropertyValues } from "../../funcs/queryQueryPropertyValues.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.PropertyValuesRequestBody$inboundSchema.optional(),
};

export const tool$queryQueryPropertyValues: ToolDefinition<typeof args> = {
  name: "query-query-property-values",
  description: `Query Property Values`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await queryQueryPropertyValues(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
