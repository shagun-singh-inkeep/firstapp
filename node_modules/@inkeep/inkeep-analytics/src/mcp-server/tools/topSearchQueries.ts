/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { topSearchQueries } from "../../funcs/topSearchQueries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TopSearchQueriesRequest$inboundSchema,
};

export const tool$topSearchQueries: ToolDefinition<typeof args> = {
  name: "top-search-queries",
  description: `Top Search Queries`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await topSearchQueries(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
