/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { weeklySearchUsers } from "../../funcs/weeklySearchUsers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.WeeklySearchUsersRequest$inboundSchema,
};

export const tool$weeklySearchUsers: ToolDefinition<typeof args> = {
  name: "weekly-search-users",
  description: `Weekly Search Users`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await weeklySearchUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
