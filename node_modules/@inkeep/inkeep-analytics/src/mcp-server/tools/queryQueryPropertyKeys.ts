/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { queryQueryPropertyKeys } from "../../funcs/queryQueryPropertyKeys.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.PropertyKeysRequestBody$inboundSchema.optional(),
};

export const tool$queryQueryPropertyKeys: ToolDefinition<typeof args> = {
  name: "query-query-property-keys",
  description: `Query Property Keys`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await queryQueryPropertyKeys(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
